import 'package:flutter/material.dart';

class AlergyCard extends StatelessWidget {
  const AlergyCard({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card();
  }
}
